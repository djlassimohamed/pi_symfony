{% extends 'base_front.html.twig' %}
{% block body %}
    <h1>Réclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reclamation.type }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reclamation.status }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reclamation.date ? reclamation.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reclamation_index') }}"class="btn btn-success">Retour a la liste</a>

    <a href="{{ path('reclamation_edit', {'id': reclamation.id}) }}" class="btn btn-primary">Editer la reclamation</a>

    {{ include('reclamation/_delete_form.html.twig')}}
{% endblock %}
