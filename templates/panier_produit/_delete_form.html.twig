<form method="post" action="{{ path('panier_produit_delete', {'id': panier_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panier_produit.id) }}">
    <button class="btn">Supprimer</button>
</form>
