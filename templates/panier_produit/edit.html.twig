{% extends 'base_front.html.twig' %}
{% block body %}
    <h1>Modification quantité</h1>

    {{ include('panier_produit/_form.html.twig', {'button_label': 'Update'}) }}
    <h4 style="color: #ff0000;">{{ error }}</h4>

    <a href="{{ path('panier_produit_index') }}">Mon panier</a>

    {{ include('panier_produit/_delete_form.html.twig') }}
{% endblock %}
