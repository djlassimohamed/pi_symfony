{% extends 'base_front.html.twig' %}
{% block body %}
    <h1>Mon produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panier_produit.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ panier_produit.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panier_produit_index') }}">Mon panier</a>

    <a href="{{ path('panier_produit_edit', {'id': panier_produit.id}) }}">Modifier</a>

    {{ include('panier_produit/_delete_form.html.twig') }}
{% endblock %}
